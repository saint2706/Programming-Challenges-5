# Linking overview

Markdown pages reference each other through standard links like [Introduction](intro.md). The visualization shows a node for each file and arrows for every link, making it easier to spot hub documents.

Consider adding more guides, then rerun the importer to index new files and refresh the graph.
