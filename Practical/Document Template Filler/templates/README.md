Sample templates are generated on demand.

Run `python "Practical/Document Template Filler/template_filler.py" --create-sample-templates` to
build `letter_template.docx` and `service_form_template.pdf` in this folder.
